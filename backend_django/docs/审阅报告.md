# backend_django 审阅报告

## 1. 审阅范围
- 目录：`backend_django/backend_django`、`backend_django/community`
- 关注点：接口契约、鉴权权限、一致性、推荐训练运维、可观测性

## 2. 总体结论
- 结论：**联调可用，生产前需安全加固**。
- 亮点：接口已覆盖前端主流程；推荐训练命令与状态记录已具备。
- 风险：开发态 `csrf_exempt` 仍较多，需上线前切换正式策略。

## 3. 分级问题清单

### P0
1. 安全策略仍偏开发态（CSRF 放开、Host/CORS 宽松）。
   - 建议：区分 dev/prod 配置，生产收敛白名单。

2. 统一错误码虽已落地，但缺少业务细分码字典。
   - 建议：定义错误码枚举并固化文档。

### P1
1. 列表与评论分页未系统化。
2. 上传接口未实现（富文本图片仍依赖前端处理）。

### P2
1. 缺少自动化测试（API/权限/回归）。
2. 缺少数据库索引优化策略说明。

## 4. 已完成关键能力
- 认证、帖子、评论、互动、推荐接口
- API 统一响应壳与 requestId
- 推荐训练状态落盘与状态查询
- 训练命令支持最小样本阈值

## 5. 建议里程碑
1. 生产安全配置落地
2. 分页与上传接口补齐
3. 自动化测试与发布流程
